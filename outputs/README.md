# Released outputs

The subdirectories `fig1` through `fig7` contain the manuscript-ready vector PDF
and a PNG preview for each final figure. Scripts write regenerated outputs to the
same locations unless an alternative `--outdir` is supplied.
